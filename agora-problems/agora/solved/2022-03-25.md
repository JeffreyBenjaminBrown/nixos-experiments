Progress! I got the requirements installed.

Next step is to run `agora/bin/pull`. It gripes that `gardens.yaml` doesn't exist. I saw that `sources.yaml` does exist, and looked like a reasonable replacement, so I tried swapping that for `gardens.yaml` in `agora/bin/pull`, and now I get this:
```
ARNING:pull:/home/jeff/agora/garden/garden/flancian doesn't exist, couldn't pull to it.
``
